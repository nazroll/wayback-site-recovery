## What & why

<!-- One or two sentences: what changes, and what problem it solves. Link the issue if there is one. -->

## Checklist

- [ ] Tests pass locally (`python -m unittest discover -s tests -v`)
- [ ] If this touches `scripts/`, tests were added or updated (and the script stays stdlib-only)
- [ ] If this touches SKILL.md or `references/`, every CLI flag / API claim was verified against the current tool version. Say how and which version below

## Verification notes

<!-- How you verified the change: recovered a real site, ran the audit on X, checked flags against pywaybackup vN.N.N, etc. -->
